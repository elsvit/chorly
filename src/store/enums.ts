export enum EStateName {
  common = 'common',
  settings = 'settings',
  parents = 'parents',
  children = 'children',
  tasks = 'tasks',
  taskBase = 'taskBase',
  taskAssignment = 'taskAssignment',
}
